---
const footer: Footer = {
    columns: [
        {
            title: "Pages",
            links: [
                {
                    text: "Home",
                    url: "/"
                },
                {
                    text: "Blogs",
                    url: "/blogs"
                },
                {
                    text: "Projects",
                    url: "/projects"
                },
            ]
        },
        {
            title: "About",
            links: [
                {
                    text: "Now",
                    url: "/now"
                }
            ]
        }
    ]
}
---

<footer>
    <div class="flex flex-row flex-wrap gap-20 container mx-auto py-16 px-16">
        { footer.columns.map((column) => (
            <div class="flex flex-col">
                <h4 class="text-3xl font-bold">{column.title}</h4>
                <div class="flex flex-col mt-2 gap-1">
                    { column.links.map((link) => (
                        <a class="hover:text-(--primary) duration-200" href={link.url}>{link.text}</a>
                    )) }
                </div>
            </div>
        )) }
    </div>

    <div class="flex border-t-2 border-neutral-200 w-full text-neutral-500 px-12 py-8 justify-evenly">
        <div>© 2025 itsfinniii / All rights reserved</div>
    </div>
</footer>